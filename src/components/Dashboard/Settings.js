import React from 'react';

const Settings = () => {

  const container = {

  };

  return (
    <div style={{ display: 'flex', flex: 1, marginTop: 50 }}>
      <h2>Settings komponentti</h2>
    </div>
  );
};

export default Settings;
